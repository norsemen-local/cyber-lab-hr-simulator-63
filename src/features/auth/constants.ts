
// Define the API URL - in production, this would come from environment variables
export const API_URL = 'http://localhost:3000/api';

// This would be the actual RDS endpoint in a real application
export const DB_ENDPOINT = "hr-portal-db.cluster-xxxxxxxxxx.us-east-1.rds.amazonaws.com";
